import 'package:flutter/material.dart';

/// REDL brand palette. Sober, premium, motorsport-heritage tone.
/// A single accent (bordeaux) used sparingly - never brightened to pure red.
class RedlColors {
  RedlColors._();

  static const base = Color(0xFF1A1A1A);
  static const baseAlt = Color(0xFFF2EFEA);

  static const accent = Color(0xFF8E2430);
  static const accentTint = Color(0xFFE6C9CC);

  static const textMuted = Color(0xFF5C5750);
  static const textSecondary = Color(0xFFB3ABA2);

  // Surface elevations on the dark base, darkest to lightest.
  static const surface0 = Color(0xFF0E0D0C);
  static const surface1 = Color(0xFF161514);
  static const surface2 = Color(0xFF221F1C);
  static const surface3 = Color(0xFF2A2825);
  static const surface4 = Color(0xFF3A3733);

  static const border = Color(0xFF3A3733);
  static const divider = Color(0xFF2A2825);
}
